{
  "name": "opencr-tests",
  "private": true,
  "scripts": {
    "test": "jest --runInBand",
    "test:e2e": "jest --runInBand --testPathPattern=e2e",
    "test:unit": "jest --runInBand --testPathPattern=unit"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "jest": "^29.7.0"
  }
}
